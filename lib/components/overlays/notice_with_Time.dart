import 'package:flutter/material.dart';

class NoticeWithTime extends StatelessWidget {
  const NoticeWithTime({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}