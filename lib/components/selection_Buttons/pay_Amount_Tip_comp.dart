import 'package:flutter/material.dart';

class PayAmountTipComp extends StatelessWidget {
  const PayAmountTipComp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}